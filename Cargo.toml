[package]
name = "ferrumcad"
version = "0.1.0"
edition = "2024"

[dependencies]
nalgebra = "0.32"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# GUI (usaremos depois)
egui = "0.27"
eframe= "0.27"
